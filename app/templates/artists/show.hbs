<h2 class="artist-name">{{model.name}}</h2>

<ul class="albums">
  {{#each model.albums key="id" as |album|}}
  {{album-li album=album}}
  {{/each}}
</ul>

<div class="back-link">{{#link-to 'artist-list'}}Back to artists{{/link-to}}</div>
